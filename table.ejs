<%- include('./navbar.html') %>

    <h1 style="color: black;"> Mongodb Data</h3>
        <br>
        <style>
            table,
            th,
            td {
                background-color: cornsilk;
                padding: 20px;
                border: 1px solid black;
                border-collapse: collapse;
            }
        </style>

        <div class="container bg-light text-dark">
            <table class="table table-striped">
                <form action="welcome" method='get'>
                    <thead>
                        <tr>
                            <th>Digit</th>
                            <th>Name</th>
                            <th>City</th>
                            
                        </tr>
                    </thead>
                    <tbody>
                        <%details.findResult.forEach(function(element) {%>
                            <tr>
                                <td>
                                    <%=element.a%>
                                </td>
                                <td>
                                    <%=element.name%>
                                </td>
                                <td>
                                    <%=element.city%>
                                </td>
                            
                            <%});%>
                    </tbody>
                </form>
            </table>
            <br>
            <% if(details.pageNo < 4) {%>
                <a style="margin-left: 50px;"
                    href="http://localhost:3000/table?page=<%=parseInt(details.pageNo)+1%>&limit=<%=details.limitValue%>">NEXT</a>
                <% } %>
                    <% if(details.pageNo> 1) {%>
                        <a style="margin-left: 100px;"
                            href="http://localhost:3000/table?page=<%=parseInt(details.pageNo)-1%>&limit=<%=details.limitValue%>">PREVIOUS</a>
                        <% } %>


        </div>